# AGENTS.md

## 目的

这个文件描述 `frontend-contracts` 的职责，帮助 Agent 判断何时应该在这里同步前端契约，而不是误把它当成业务前端或后端实现仓。

## 项目定位

- 这是前端共享契约子仓。
- 它保存给前后端共同消费的 OpenAPI 契约，以及可复用的 API / TypeScript 类型产物。
- 它是接口契约的统一发布层。
- 默认分支为 `master`。

## 相对工作台后端业务仓的能力边界

这个子仓负责“同步和分发契约”，不负责真正实现接口。

这个子仓能直接处理：

- 维护 `openapi/*.yaml` 契约源文件
- 维护 `src/apis`、`src/types` 这类共享契约代码
- 统一前端对字段名、结构、枚举的消费方式

这个子仓不能单独完成：

- 新增/修改后端 Gin API
- 修改 DTO、响应拼装、错误码、领域逻辑
- 修复数据库、缓存、权限、排序等服务端行为

如果接口真实语义发生变化，源头一定先在工作台业务仓 `business-repo/backend-superone` 修改实现；这个仓只负责跟进同步并发布契约。

## 代码入口

- OpenAPI：`openapi/*.yaml`
- 共享 API：`src/apis/*`
- 共享类型：`src/types/*`

## 何时优先改这个仓

满足任一即可：

- 后端 OpenAPI 已改，需要同步前端消费文件
- 多个前端项目都要共用同一套接口类型
- 某个字段、枚举、响应结构已经在后端确定，只差前端契约更新

## 何时不能只改这个仓

满足任一即可：

- 后端接口代码还没改
- 文档字段与真实返回不一致，根因在主仓实现
- 错误码、默认值、排序、分页语义本身要变

## 联动顺序

推荐按这个顺序处理：

1. `business-repo/backend-superone` 修改接口实现
2. 当前仓同步 `openapi/*.yaml`
3. 当前仓同步 `src/apis` / `src/types`
4. 受影响前端项目再落具体页面改动

## 常见注意点

- 不要把当前仓当成后端实现来源；来源永远是 `business-repo/backend-superone` 实现与当前仓契约文件。
- 不要只改共享类型却不核对业务前端是否真的已消费新字段。
- 如果只是单个前端页面的本地展示逻辑变化，通常不需要动这个仓。
